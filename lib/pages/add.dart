import 'package:flutter/material.dart';

import 'package:goodbye_money/types/widgets.dart';

class Add extends WidgetWithTitle {
  const Add({Key? key}) : super(key: key, title: "Add");

  @override
  Widget build(BuildContext context) {
    return Text("Add!");
  }
}
